## Proyecto integrador CMS Multipaís

> Paso 1: crear proyecto backend

mkdir backend
cd backend
npm init -y
npm install express cors dotenv @supabase/supabase-js bcryptjs jsonwebtoken
npm install -D nodemon

> Paso 2: configurar package.json
Reemplazar el contenido en backend/package.json

{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend CMS administrativo multipais",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "dev": "nodemon src/server.js",
    "start": "node src/server.js",
    "create-superadmin": "node src/scripts/createSuperAdmin.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.0.0",
    "bcryptjs": "^3.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^5.0.0",
    "jsonwebtoken": "^9.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  }
}

> Paso 3: estructura inicial (con esta comienza la capa backend)
backend/
├── src/
│   ├── config/
│   │   └── supabase.js
│   ├── controllers/
│   ├── middlewares/
│   ├── routes/
│   ├── scripts/
│   ├── services/
│   └── server.js
├── .env
└── package.json

> Paso 4: archivo .env (configuración del archivo)

-  crear el archivo en backend/.env: 
_____________________________________________________________ 
PORT=3001

SUPABASE_URL=SUPABASE_URL
SUPABASE_ANON_KEY=SUPABASE_ANON_KEY
SUPABASE_SERVICE_ROLE_KEY=SUPABASE_SERVICE_ROLE_KEY

JWT_SECRET=clave_super_segura_cms_multipais_santotomas_26
JWT_EXPIRES_IN=2h
_____________________________________________________________

> rutas para copiar la configuración de conexión de supabase 
- SUPABASE_URL
[Ruta: project settings/ Data API/ API URL (copiar solo hasta el .co)
ejemplo:
https://ktdnkabcxbptsaqsnowi.supabase.co/

- SUPABASE_ANON_KEY
Ruta: project settings/ API Key/ Legacy anon, service_role API keys/ anon public/ copy
ejemplo:
eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Imt0ZG5

- SUPABASE_SERVICE_ROLE_KEY
[Ruta: project settings/ API Key/ Legacy anon, service_role API keys/ service_role secret/ Reveal/ Copy
ejemplo:
eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Imt0ZG5rs

> Paso 5: conexión Supabase
Archivo:
backend/src/config/supabase.js

> Paso 6: servidor base
Archivo:
backend/src/server.js

> Paso 7: probar backend
npm run dev

> Paso 7.1 En Postman:
- GET 
http://localhost:3001/

- Respuesta esperada:
{
  "message": "API CMS multipais funcionando correctamente"
}

> Paso 8: crear tablas en Supabase (SQL Editor - al crear la tablas seleccionar: Run and enable RLS)

create table if not exists paises (
  id bigint generated by default as identity primary key,
  nombre text not null,
  codigo text not null unique,
  slug text not null unique,
  estado text not null default 'activo',
  created_at timestamptz default now(),
  updated_at timestamptz default now()
);

create table if not exists roles (
  id bigint generated by default as identity primary key,
  nombre text not null unique,
  descripcion text,
  created_at timestamptz default now()
);

create table if not exists usuarios (
  id bigint generated by default as identity primary key,
  nombre text not null,
  apellido text not null,
  email text not null unique,
  username text not null unique,
  password_hash text not null,
  rol_id bigint not null references roles(id),
  pais_id bigint references paises(id),
  estado text not null default 'activo',
  ultimo_acceso timestamptz,
  created_at timestamptz default now(),
  updated_at timestamptz default now()
);

> tener en cuenta los alter table usuarios de los pasos 22 y 22.1

* Estas tablas corresponden al modelo base del documento: paises, roles y usuarios, donde el usuario tiene rol, país asignado y contraseña cifrada.

> Paso 9: insertar datos iniciales (SQL Editor supabase)

insert into paises (nombre, codigo, slug, estado)
values
('Argentina', 'AR', 'argentina', 'activo'),
('Chile', 'CL', 'chile', 'activo'),
('Ecuador', 'EC', 'ecuador', 'activo')
on conflict (codigo) do nothing;

insert into roles (nombre, descripcion)
values
('superadmin', 'Administra todo el sistema y todos los países'),
('admin_pais', 'Administra únicamente el portal del país asignado'),
('editor', 'Crea y edita contenido, pero no administra usuarios')
on conflict (nombre) do nothing;

> Paso 10: activar RLS
alter table public.paises enable row level security;
alter table public.roles enable row level security;
alter table public.usuarios enable row level security;

* Como el backend usará supabaseAdmin, podremos operar desde servidor sin abrir las tablas al público.

> Paso 11: script para crear superadmin

Archivo:
backend/src/scripts/createSuperAdmin.js

> Paso 11.1: Ejecutar en una nueva terminal del backend:
npm run create-superadmin

> Paso 12: Crear servicio
Archivo:
backend/src/services/authService.js

> Paso 13: Crear controlador
Archivo:
backend/src/controllers/authController.js

> Paso 14: Crear rutas
Archivo:
backend/src/routes/authRoutes.js

> Paso 15: Registrar rutas en server.js
Archivo:
backend/src/server.js


> Paso 16: Probar en Postman
- Método:
POST

- URL:
http://localhost:3001/api/auth/login

- Body → raw → JSON:
{
  "username": "superadmin",
  "password": "superadmin123*"
}

- Respuesta esperada
{
  "message": "Inicio de sesión exitoso",
  "token": "eyJhbGciOiJIUzI1NiIs...",
  "user": {
    "id": 1,
    "nombre": "Sergio",
    "apellido": "Puerto",
    "email": "superadmin@demo.com",
    "username": "superadmin",
    "rol": "superadmin",
    "pais": null
  }
}

> Paso 17: Crear middleware JWT para GET /api/paises 
- A lo que vamos a llegar:
Login → JWT → Middleware → Ruta protegida → Filtro por rol/país

Archivo:
backend/src/middlewares/authMiddleware.js

> Paso 17.1: Crear servicio de países
Archivo:
backend/src/services/paisService.js

> Paso 17.2: Crear controlador
Archivo:
backend/src/controllers/paisController.js

> Paso 17.3: Crear rutas
Archivo:
backend/src/routes/

> Paso 17.4: Actualizar server.js
import paisRoutes from './routes/paisRoutes.js';
app.use('/api/paises', paisRoutes);

> Paso 17.5: Probar en Postman sin token
- GET 
http://localhost:3001/api/paises

- Respuesta esperada:
{
  "error": "Token no proporcionado"
}

> Paso 17.6: Probar en Postman con token
Primero hacer login (en una pestaña):
- POST 
http://localhost:3001/api/auth/login

- Body:
{
  "username": "superadmin",
  "password": "superadmin123*"
}

- Copiar el token.

- Luego (en otra pestaña):

- GET 
- http://localhost:3001/api/paises

- parametrizar:
Authorization → Bearer Token → pega el token

> Paso 18: Creamos usuarios de prueba para validar el filtro por rol + país.

admin_argentina / admin123*
editor_argentina / editor123*

- Crear script
backend/src/scripts/createTestUsers.js

- Agregar script en package.json
En backend/package.json, agregar:
"create-test-users": "node src/scripts/createTestUsers.js"

- Debe quedar así:
"scripts": {
  "dev": "nodemon src/server.js",
  "start": "node src/server.js",
  "create-superadmin": "node src/scripts/createSuperAdmin.js",
  "create-test-users": "node src/scripts/createTestUsers.js"
}

- Ejecutar en una nueva terminal de backend:
npm run create-test-users

- Respuesta esperada:
Usuario creado: admin_argentina
Contraseña: admin123*
Usuario creado: editor_argentina
Contraseña: editor123*
Proceso finalizado.

> Paso 18.1: Probar login admin_pais en Postman:

- POST 
http://localhost:3001/api/auth/login

- Body:
{
  "username": "admin_argentina",
  "password": "admin123*"
}

> Paso 18.2: Probar filtro de países

Con el token de admin_argentina:

- GET http://localhost:3001/api/paises

- Authorization:
Bearer TOKEN_ADMIN_ARGENTINA

- Debe devolver solo Argentina

> Paso 18.3: Probar login editor

- POST http://localhost:3001/api/auth/login

- Body:
{
  "username": "editor_argentina",
  "password": "editor123*"
}

- Luego probamos filtro de países con el token del rol de editor:

GET http://localhost:3001/api/paises

También debe devolver solo Argentina.

- Con esto ya validamos:
+  superadmin → ve todos los países
+  admin_pais → ve solo su país
+  editor → ve solo su país


> Paso 19: Crear las rutas de administración de usuarios

Las rutas de usuarios quedan protegidas para que solo el rol `superadmin` pueda listar, crear, editar, desactivar y eliminar usuarios.

Reglas aplicadas:

- El `superadmin` administra usuarios.
- `admin_pais` y `editor` no administran usuarios.
- No se permite que un usuario se desactive a sí mismo.
- No se permite que un usuario se elimine físicamente a sí mismo.
- No se permite eliminar físicamente usuarios con rol `superadmin`.
- La eliminación lógica cambia el estado del usuario a `inactivo`.
- La eliminación física borra el registro de la tabla `usuarios`.



> paso 19.1: Crear userService.js
backend/src/services/userService.js
> paso 19.2: Crear userController.js
backend/src/controllers/userController.js
> paso 19.3: Crear userRoutes.js
backend/src/routes/userRoutes.js
> paso 19.4: Actualizar server.js
backend/src/server.js

import userRoutes from './routes/userRoutes.js';
app.use('/api/users', userRoutes);

> Paso 19.5: Pruebas en Postman: listar usuarios desde el rol SuperAdmin

- Iniciar sesión como superadmin:

- POST http://localhost:3001/api/auth/login
{
  "username": "superadmin",
  "password": "superadmin123*"
}

- Copia el token.
- Listar usuarios
GET http://localhost:3001/api/users

- Authorization:
Bearer TOKEN_SUPERADMIN

> Paso 19.6: Pruebas en Postman: Crear usuario admin país

- POST http://localhost:3001/api/users
- Authorization:
Bearer TOKEN_SUPERADMIN

- Body:
{
  "nombre": "Admin",
  "apellido": "Chile",
  "email": "admin.chile@demo.com",
  "username": "admin_chile",
  "password": "admin123*",
  "rol_id": 2,
  "pais_id": 2,
  "estado": "activo"
}

- Respuesta esperada:
Usuario creado correctamente

> Paso 19.7: Pruebas en Postman: Crear usuario editor
- POST http://localhost:3001/api/users
- Authorization:
Bearer TOKEN_SUPERADMIN

- Body:
{
  "nombre": "Editor",
  "apellido": "Ecuador",
  "email": "editor.ecuador@demo.com",
  "username": "editor_ecuador",
  "password": "editor123*",
  "rol_id": 3,
  "pais_id": 3,
  "estado": "activo"
}
- Respuesta esperada:
Usuario creado correctamente

> Paso 19.8: Pruebas en Postman: Editar usuario (datos completos)
- PUT http://localhost:3001/api/users/2
- Authorization:
Bearer TOKEN_SUPERADMIN

- Body:
{
  "nombre": "Admin",
  "apellido": "Argentina Actualizado",
  "email": "admin.argentina@demo.com",
  "username": "admin_argentina",
  "rol_id": 2,
  "pais_id": 1,
  "estado": "activo"
}
- Respuesta esperada:
Usuario actualizado correctamente

> Paso 19.9: Pruebas en Postman: Editar usuario - (datos completos) Cambiar contraseña de usuario
- PUT http://localhost:3001/api/users/2
- Authorization:
Bearer TOKEN_SUPERADMIN

- Body:
{
  "nombre": "Admin",
  "apellido": "Argentina",
  "email": "admin.argentina@demo.com",
  "username": "admin_argentina",
  "password": "NuevaClave123*",
  "rol_id": 2,
  "pais_id": 1,
  "estado": "activo"
}


> Paso 19.10: Pruebas en Postman: Desactivar usuario
DELETE http://localhost:3001/api/users/2

Esto no borra físicamente: solo cambia:

{
  "estado": "inactivo"
}

> Paso 19.11: Pruebas en Postman: Eliminar usuario definitivamente
- DELETE http://localhost:3001/api/users/2/permanent

- Authorization:
Bearer TOKEN_SUPERADMIN

- Resultado:
{
  "message": "Usuario eliminado definitivamente"
}

## v1. MAYO

> Actualización de la información de los usuarios
PUT /api/users/:id → actualización completa.
PATCH /api/users/:id → actualización parcial.


>Paso 20: PATCH parcial (actualización parcial)
- Login
  
- PATCH 
http://localhost:3001/api/users/3

- Authorization:
Bearer TOKEN

- body/ raw/ json
{
  "nombre": "Juan actualizado"
}




>Paso 21: PATCH parcial - Actualizar mi perfil
- PATCH 
http://localhost:3001/api/auth/me

- Authorization:
Bearer TOKEN_ADMIN_O_EDITOR

- body/raw/json:
{
  "nombre": "Admin Arg actualizado"
}

> Paso 21.1: Cambiar el rol de un usuario a "Super Admin"
- PATCH
http://localhost:3001/api/users/id
- http://localhost:3001/api/users/3

- Login
SUPER_ADMIN
- Authorization:
Bearer TOKEN

- body/raw/json:

{
  "rol_id": 1,
}


> Paso 21.2: Cambiar el rol de un usuario a "Admin país o Editor" 
- PATCH
http://localhost:3001/api/users/id
- http://localhost:3001/api/users/3

- Login
SUPER_ADMIN
- Authorization:
Bearer TOKEN

- body/raw/json:

{
  "rol_id": 3,
  "pais_id":1
}

>Paso 21.3: Intentar mi propio rol (según la lógica del negocio NO se puede)
- PATCH 
http://localhost:3001/api/auth/me

- Authorization:
Bearer TOKEN_ADMIN_O_EDITOR

- body/raw/json:
{
  "rol_id": 1
}

> Paso 22: SQL: agregar pregunta y respuesta de seguridad 

- La respuesta de seguridad se guarda con hash, 
- todos pueden recuperar por olvido, 
- cualquier usuario autenticado cambia su propia contraseña y 
- solo superadmin cambia la de otros. 

> Lo hacemos con 3 flujos:

1.) Olvidé mi contraseña
POST /api/auth/forgot-password

3.) Cambiar mi propia contraseña
PATCH /api/auth/change-my-password

5.)Superadmin cambia contraseña de otro usuario
PATCH /api/users/:id/password


> vamos al SQL editor de supabase:
alter table usuarios
add column if not exists pregunta_seguridad text,
add column if not exists respuesta_seguridad_hash text;

> Se actualiza:
userService.js
authService.js
authController.js
authRoutes.js
userRoutes.js

> Pruebas Postman:

## 1. CONFIGURAR / CAMBIAR MI PREGUNTA DE SEGURIDAD-

- Login (Endpoint para editor_argentina)

- PATCH 
http://localhost:3001/api/auth/security-question

- Headers
Authorization: Bearer TOKEN_USUARIO
Content-Type: application/json

- Body
{
  "pregunta": "¿Cuál es el nombre de tu primera mascota?",
  "respuesta": "firulais"
}

- Respuesta esperada
{
  "message": "Pregunta de seguridad configurada correctamente",
  "user": {
    "id": 3,
    "username": "editor_argentina",
    "pregunta_seguridad": "¿Cuál es el nombre de tu primera mascota?"
  }
} 

## 2. CONSULTAR PREGUNTA DE SEGURIDAD (ANTES DE RECUPERAR)
- Endpoint
POST http://localhost:3001/api/auth/security-question

- Body
{
  "username": "editor_argentina"
}

- Respuesta esperada
{
  "username": "editor_argentina",
  "pregunta_seguridad": "¿Cuál es el nombre de tu primera mascota?"
}

## 3. RECUPERAR CONTRASEÑA (OLVIDO)
- Endpoint
POST http://localhost:3001/api/auth/forgot-password

- Body
{
  "username": "editor_argentina",
  "respuesta": "firulais",
  "newPassword": "NuevaClave123*"
}

- Respuesta esperada
{
  "message": "Contraseña restaurada correctamente"
}

## 4. CAMBIAR MI PROPIA CONTRASEÑA

- Endpoint
PATCH 
http://localhost:3001/api/auth/change-my-password

- Headers
Authorization: Bearer TOKEN_USUARIO
Content-Type: application/json

- Body
{
  "currentPassword": "NuevaClave123*",
  "newPassword": "OtraClave123*"
}
- Respuesta esperada
{
  "message": "Contraseña actualizada correctamente"
}

## 5. SUPERADMIN CAMBIA CONTRASEÑA DE OTRO USUARIO

- Endpoint
Primero hacer login (en una pestaña):

- POST 
http://localhost:3001/api/auth/login

- Body:
{
  "username": "superadmin",
  "password": "superadmin123*"
}

luego:
PATCH 
http://localhost:3001/api/users/3/password

- Headers
Authorization: Bearer TOKEN_SUPERADMIN
Content-Type: application/json

- Body
{
  "newPassword": "ClaveAdmin123*"
}

- Respuesta esperada
{
  "message": "Contraseña del usuario actualizada correctamente",
  "user": {
    "id": 3,
    "username": "editor_argentina",
    "estado": "activo"
  }
}

### v2 MAYO: seguridad (anti fuerza bruta)

> Para los intentos de inicio de sesión vamos a tener presente
1. Máximo 3 intentos fallidos
2. Bloqueo por 10 segundos
3. Reset de intentos al login correcto
4. Mensaje claro al usuario


> PASO 22.1: Validación de intentos de sesión

> 1. SQL (SUPABASE):

alter table usuarios
add column if not exists login_intentos int default 0,
add column if not exists login_bloqueado_hasta timestamptz;

> 2. Se actualiza authService.js

> 3. PRUEBAS EN POSTMAN

- POST 
http://localhost:3001/api/auth/login

- Body con Intentos fallidos
{
  "username": "editor_argentina",
  "password": "mala123"
}

- Vamos a Hacer esto 3 veces

- Resultado esperado (4to intento)
{
  "error": "Cuenta bloqueada temporalmente. Intenta en 10 segundos"
}

- Esperar 10 segundos

- Luego intentar login correcto:

{
  "username": "editor_argentina",
  "password": "editor123*"
}

- Debe dejar entrar

> PASO 23: Módulo Noticias
Según el documento del PI, noticias debe permitir: 
- crear, editar, eliminar, asignar país, publicar/despublicar y filtrar según rol y país. 
- Además, superadmin ve todo, mientras admin_pais y editor solo ven su país. 

> PASO 23.1 SQL en Supabase
- Supabase → SQL Editor (Run and enable RLS):
create table if not exists noticias (
  id bigint generated by default as identity primary key,

  pais_id bigint not null references paises(id),
  titulo text not null,
  slug text not null,
  resumen text not null,
  contenido text not null,
  imagen_principal_url text,

  autor_id bigint not null references usuarios(id),

  estado text not null default 'borrador',
  fecha_publicacion timestamptz,

  created_at timestamptz default now(),
  updated_at timestamptz default now(),

  constraint noticias_estado_check
  check (estado in ('borrador', 'publicado', 'despublicado')),

  constraint noticias_slug_pais_unique
  unique (pais_id, slug)
);

- Activar RLS:
alter table public.noticias enable row level security;

> 23.2 Se crea noticiaService.js
backend/src/services/noticiaService.js

> 23.3 Crear noticiaController.js
backend/src/controllers/noticiaController.js

> 23.4 Crear noticiaRoutes.js
backend/src/routes/noticiaRoutes.js

> 23.5 Actualizar server.js
backend/src/server.js

> 23.6 Reiniciar backend
npm run dev

> 23.7 Pruebas en Postman

Primero iniciar sesión como superadmin:

- POST 
http://localhost:3001/api/auth/login

- Body:
{
  "username": "superadmin",
  "password": "superadmin123*"
}
- Copiar el token.

> A. Crear noticia como superadmin

- POST 
- http://localhost:3001/api/noticias

- Authorization:
Bearer TOKEN_SUPERADMIN

- Body:
{
  "pais_id": 1,
  "titulo": "Nueva jornada de formación en Argentina",
  "resumen": "Resumen de la noticia de formación.",
  "contenido": "Contenido completo de la noticia publicada para el portal de Argentina.",
  "imagen_principal_url": "https://ejemplo.com/imagen.jpg",
  "estado": "borrador"
}

- Respuesta esperada:
{
  "message": "Noticia creada correctamente"
}

> B. Listar noticias
-GET 
http://localhost:3001/api/noticias

- Authorization:
Bearer TOKEN_SUPERADMIN

- Respuesta esperada:
[
  {
    "id": 1,
    "titulo": "Nueva jornada de formación en Argentina",
    "slug": "nueva-jornada-de-formacion-en-argentina",
    "estado": "borrador"
  }
]

> C. Ver detalle
- GET 
http://localhost:3001/api/noticias/1

- Authorization:
Bearer TOKEN_SUPERADMIN

> D. Editar noticia completa con PUT
- PUT 
http://localhost:3001/api/noticias/1

- Authorization:
Bearer TOKEN_SUPERADMIN

- Body:
{
  "pais_id": 1,
  "titulo": "Nueva jornada de formación actualizada en Argentina",
  "resumen": "Resumen actualizado de la noticia.",
  "contenido": "Contenido completo actualizado de la noticia.",
  "imagen_principal_url": "https://ejemplo.com/imagen-actualizada.jpg",
  "estado": "borrador"
}

> E. Editar noticia parcial con PATCH
- PATCH 
http://localhost:3001/api/noticias/1

- Authorization:
Bearer TOKEN_SUPERADMIN

- Body:
{
  "titulo": "Título actualizado parcialmente"
}

- Respuesta esperada:
{
  "message": "Noticia actualizada parcialmente"
}

> F. Publicar noticia
- PATCH 
http://localhost:3001/api/noticias/1/estado

- Authorization:
Bearer TOKEN_SUPERADMIN

- Body:
{
  "estado": "publicado"
}

- Respuesta esperada:
{
  "message": "Estado de noticia actualizado correctamente",
  "noticia": {
    "id": 1,
    "estado": "publicado"
  }
}

> G. Despublicar noticia
- PATCH http://localhost:3001/api/noticias/1/estado

- Authorization:
Bearer TOKEN_SUPERADMIN

- Body:
{
  "estado": "despublicado"
}

> H. Probar filtro por país con admin_pais

- Login como admin_argentina:
- POST http://localhost:3001/api/auth/login

- Body:
{
  "username": "admin_argentina",
  "password": "admin123*"
}

- Luego:
GET http://localhost:3001/api/noticias

- Authorization:

- Bearer TOKEN_ADMIN_ARGENTINA

- Debe mostrar solo noticias de Argentina.

> I. Admin_pais no puede crear noticia de otro país

- Con token de admin_argentina:

- POST http://localhost:3001/api/noticias

- Body:
{
  "pais_id": 2,
  "titulo": "Noticia para Chile",
  "resumen": "Resumen de prueba.",
  "contenido": "Contenido de prueba.",
  "imagen_principal_url": "",
  "estado": "borrador"
}

- Respuesta esperada:
{
  "error": "No puedes crear noticias para un país diferente al asignado"
}


> J. Editor puede crear y editar, pero no eliminar

- Login como editor_argentina:
{
  "username": "editor_argentina",
  "password": "editor123*"
}

- Crear noticia:
- POST http://localhost:3001/api/noticias

- Body:
{
  "pais_id": 1,
  "titulo": "Noticia creada por editor",
  "resumen": "Resumen creado por editor.",
  "contenido": "Contenido creado por editor.",
  "estado": "borrador"
}
- Debe permitir.

- Ahora vamos a intentar eliminar:
DELETE http://localhost:3001/api/noticias/1

- Respuesta esperada:
{
  "error": "No tienes permisos para realizar esta acción"
}

> K. Eliminar noticia como superadmin o admin_pais
- POST 
http://localhost:3001/api/auth/login

- Body:
{
  "username": "superadmin",
  "password": "superadmin123*"
}
- Copiar el token.
  
- DELETE http://localhost:3001/api/noticias/1

- Authorization:
Bearer TOKEN_SUPERADMIN

- Respuesta esperada:
{
  "message": "Noticia eliminada correctamente"
}

> Rutas del módulo Noticias
GET     /api/noticias
GET     /api/noticias/:id
POST    /api/noticias
PUT     /api/noticias/:id
PATCH   /api/noticias/:id
PATCH   /api/noticias/:id/estado
DELETE  /api/noticias/:id

> PASO 24: Módulo Testimonios

> 24.1 SQL en Supabase

- SQL Editor:
create table if not exists testimonios (
  id bigint generated by default as identity primary key,

  pais_id bigint not null references paises(id),

  nombre text not null,
  cargo text,
  empresa text,
  contenido text not null,
  foto_url text not null,

  instagram_url text,
  facebook_url text,

  estado text not null default 'borrador',
  destacado boolean not null default false,

  autor_id bigint not null references usuarios(id),
  fecha_publicacion timestamptz,

  created_at timestamptz default now(),
  updated_at timestamptz default now(),

  constraint testimonios_estado_check
  check (estado in ('borrador', 'publicado', 'despublicado'))
);

- Activar RLS:
alter table public.testimonios enable row level security;

> 24.2 Crear testimonioService.js
backend/src/services/testimonioService.js
> 24.3 Crear testimonioController.js
backend/src/controllers/testimonioController.js
> 24.4 Crear testimonioRoutes.js
backend/src/routes/testimonioRoutes.js
> 24.5 Actualizar server.js
backend/src/server.js
> 24.6 Reiniciar backend
npm run dev

> 24.7 Pruebas en Postman

- Primero inicia sesión como superadmin.

- POST http://localhost:3001/api/auth/login

- Body:
{
  "username": "superadmin",
  "password": "superadmin123*"
}

- Copiar el token.

> A. Crear testimonio como superadmin
- POST http://localhost:3001/api/testimonios

- Authorization:
Bearer TOKEN_SUPERADMIN

- Body:
{
  "pais_id": 1,
  "nombre": "María Fernández",
  "cargo": "Directora de proyecto",
  "empresa": "Fundación Aprende",
  "contenido": "La experiencia con el programa fue transformadora para nuestra comunidad.",
  "foto_url": "https://ejemplo.com/foto-maria.jpg",
  "instagram_url": "https://instagram.com/maria",
  "facebook_url": "https://facebook.com/maria",
  "estado": "borrador",
  "destacado": true
}

- Respuesta esperada:
{
  "message": "Testimonio creado correctamente"
}

> B. Listar testimonios
- GET http://localhost:3001/api/testimonios

- Authorization:
Bearer TOKEN_SUPERADMIN

> C. Ver detalle
- GET http://localhost:3001/api/testimonios/1

-Authorization:
Bearer TOKEN_SUPERADMIN


> D. Editar testimonio completo con PUT
- PUT http://localhost:3001/api/testimonios/1

- Authorization:
Bearer TOKEN_SUPERADMIN

- Body:
{
  "pais_id": 1,
  "nombre": "María Fernández Actualizada",
  "cargo": "Coordinadora general",
  "empresa": "Fundación Aprende",
  "contenido": "La experiencia actualizada fue muy positiva para nuestra comunidad.",
  "foto_url": "https://ejemplo.com/foto-maria-actualizada.jpg",
  "instagram_url": "https://instagram.com/mariaactualizada",
  "facebook_url": "https://facebook.com/mariaactualizada",
  "estado": "borrador",
  "destacado": false
}

> E. Editar testimonio parcial con PATCH
- PATCH http://localhost:3001/api/testimonios/1

- Authorization:
Bearer TOKEN_SUPERADMIN

- Body:
{
  "destacado": true
}

- Respuesta esperada:
{
  "message": "Testimonio actualizado parcialmente"
}

> F. Publicar testimonio
- PATCH http://localhost:3001/api/testimonios/1/estado

- Authorization:
Bearer TOKEN_SUPERADMIN

- Body:
{
  "estado": "publicado"
}

- Respuesta esperada:
{
  "message": "Estado de testimonio actualizado correctamente"
}

> G. Despublicar testimonio
- PATCH http://localhost:3001/api/testimonios/1/estado

- Body:
{
  "estado": "despublicado"
}

> H. Probar filtro por país con admin_pais

- Login como admin_argentina:

- POST http://localhost:3001/api/auth/login

- Body:
{
  "username": "admin_argentina",
  "password": "admin123*"
}

- Luego:
GET http://localhost:3001/api/testimonios

- Authorization:
Bearer TOKEN_ADMIN_ARGENTINA

- Debe mostrar solo testimonios de Argentina.

> I. Admin_pais no puede crear testimonio de otro país

- Con token de admin_argentina:

- POST http://localhost:3001/api/testimonios

- Body:
{
  "pais_id": 2,
  "nombre": "Testimonio Chile",
  "contenido": "Este testimonio intenta crearse en otro país.",
  "foto_url": "https://ejemplo.com/foto.jpg",
  "estado": "borrador",
  "destacado": false
}

- Respuesta esperada:
{
  "error": "No puedes crear testimonios para un país diferente al asignado"
}

> J. Editor puede crear y editar, pero no eliminar

- Login como editor_argentina:
{
  "username": "editor_argentina",
  "password": "editor123*"
}

- Crear testimonio:
POST http://localhost:3001/api/testimonios

- Body:
{
  "pais_id": 1,
  "nombre": "Testimonio creado por editor",
  "cargo": "Participante",
  "empresa": "Comunidad Local",
  "contenido": "Este testimonio fue creado por un editor.",
  "foto_url": "https://ejemplo.com/editor.jpg",
  "estado": "borrador",
  "destacado": false
}
- Debe permitir.

- Intentar eliminar:
DELETE http://localhost:3001/api/testimonios/1

- Respuesta esperada:
{
  "error": "No tienes permisos para realizar esta acción"
}

> K. Eliminar testimonio como superadmin o admin_pais
- DELETE http://localhost:3001/api/testimonios/1

- Authorization:
Bearer TOKEN_SUPERADMIN

- Respuesta esperada:
{
  "message": "Testimonio eliminado correctamente"
}

> Rutas del módulo Testimonios
GET     /api/testimonios
GET     /api/testimonios/:id
POST    /api/testimonios
PUT     /api/testimonios/:id
PATCH   /api/testimonios/:id
PATCH   /api/testimonios/:id/estado
DELETE  /api/testimonios/:id

> PASO 25: Módulo Solicitudes de contacto

>25.1 SQL en Supabase

- SQL Editor:

create table if not exists solicitudes_contacto (
  id bigint generated by default as identity primary key,

  pais_id bigint not null references paises(id),

  nombre text not null,
  correo text not null,
  telefono text not null,
  finalidad text not null,
  mensaje text,

  estado text not null default 'pendiente',

  observaciones_admin text,
  fecha_gestion timestamptz,
  gestionado_por bigint references usuarios(id),

  created_at timestamptz default now(),
  updated_at timestamptz default now(),

  constraint solicitudes_estado_check
  check (estado in ('pendiente', 'en_proceso', 'gestionada', 'cerrada')),

  constraint solicitudes_finalidad_check
  check (finalidad in ('Servicio', 'Programa EDIFICA', 'Shows y conferencias'))
);

- Activar RLS:
alter table public.solicitudes_contacto enable row level security;


> 25.2 Crear solicitudService.js
backend/src/services/solicitudService.js

> 25.3 Crear solicitudController.js
backend/src/controllers/solicitudController.js

> 25.4 Crear solicitudRoutes.js
backend/src/routes/solicitudRoutes.js

>25.5 Actualizar server.js
backend/src/server.js

> 25.6 Reiniciar backend
npm run dev


> 25.7 Pruebas en Postman

> A. Crear solicitud pública

- Esta ruta no lleva token.
POST http://localhost:3001/api/solicitudes/public

- Body:
{
  "pais_id": 1,
  "nombre": "Carlos Ramírez",
  "correo": "carlos.ramirez@demo.com",
  "telefono": "3001234567",
  "finalidad": "Servicio",
  "mensaje": "Deseo recibir información sobre los servicios disponibles."
}

- Respuesta esperada:
{
  "message": "Solicitud enviada correctamente",
  "solicitud": {
    "id": 1,
    "estado": "pendiente"
  }
}

> B. Crear solicitud con correo inválido
- POST http://localhost:3001/api/solicitudes/public

-Body:
{
  "pais_id": 1,
  "nombre": "Carlos Ramírez",
  "correo": "correo-mal",
  "telefono": "3001234567",
  "finalidad": "Servicio"
}

- Respuesta esperada:
{
  "error": "El formato del correo no es válido"
}

> C. Listar solicitudes como superadmin

- Primero login:

- POST http://localhost:3001/api/auth/login

- Body:
{
  "username": "superadmin",
  "password": "superadmin123*"
}

- Luego:
GET http://localhost:3001/api/solicitudes

- Authorization:
Bearer TOKEN_SUPERADMIN
- Debe mostrar todas las solicitudes.

> D. Ver detalle de solicitud
- GET http://localhost:3001/api/solicitudes/1

- Authorization:
Bearer TOKEN_SUPERADMIN

> E. Editar solicitud completa con PUT
- PUT http://localhost:3001/api/solicitudes/1

- Authorization:
Bearer TOKEN_SUPERADMIN

- Body:
{
  "pais_id": 1,
  "nombre": "Carlos Ramírez Actualizado",
  "correo": "carlos.actualizado@demo.com",
  "telefono": "3010000000",
  "finalidad": "Programa EDIFICA",
  "mensaje": "Deseo información actualizada.",
  "estado": "en_proceso",
  "observaciones_admin": "Se inició contacto con el usuario."
}

> F. Editar solicitud parcial con PATCH
- PATCH http://localhost:3001/api/solicitudes/1

- Authorization:
Bearer TOKEN_SUPERADMIN

- Body:
{
  "telefono": "3022222222"
}

- Respuesta esperada:
{
  "message": "Solicitud actualizada parcialmente"
}

> G. Cambiar estado de solicitud
- PATCH http://localhost:3001/api/solicitudes/1/estado

- Authorization:
Bearer TOKEN_SUPERADMIN

- Body:
{
  "estado": "gestionada",
  "observaciones_admin": "Solicitud gestionada correctamente por llamada telefónica."
}

- Respuesta esperada:
{
  "message": "Estado de solicitud actualizado correctamente"
}


> H. Probar filtro por país con admin_pais

- Login como admin_argentina:
POST http://localhost:3001/api/auth/login

- Body:
{
  "username": "admin_argentina",
  "password": "admin123*"
}

- Luego:
GET http://localhost:3001/api/solicitudes

- Authorization:
Bearer TOKEN_ADMIN_ARGENTINA

- Debe mostrar solo solicitudes de Argentina.

> I. Admin_pais no puede mover solicitud a otro país

- Con token de admin_argentina:
PATCH http://localhost:3001/api/solicitudes/1

- Body:
{
  "pais_id": 2
}

- Respuesta esperada:
{
  "error": "No puedes mover solicitudes a un país diferente al asignado"
}

> J. Editor no puede gestionar solicitudes

- Login como editor_argentina:
{
  "username": "editor_argentina",
  "password": "editor123*"
}

- Luego:
GET http://localhost:3001/api/solicitudes

- Respuesta esperada:
{
  "error": "No tienes permisos para realizar esta acción"
}

> K. Eliminar solicitud
- DELETE http://localhost:3001/api/solicitudes/1

- Authorization:
Bearer TOKEN_SUPERADMIN

- Respuesta esperada:
{
  "message": "Solicitud eliminada correctamente"
}

> Rutas del módulo Solicitudes
POST    /api/solicitudes/public
GET     /api/solicitudes
GET     /api/solicitudes/:id
PUT     /api/solicitudes/:id
PATCH   /api/solicitudes/:id
PATCH   /api/solicitudes/:id/estado
DELETE  /api/solicitudes/:id

### v3 DE MAYO:
> Paso 26: Bitácora de auditoría

>26.1 SQL en Supabase:
create table if not exists bitacora_auditoria (
  id bigint generated by default as identity primary key,

  usuario_id bigint references usuarios(id),
  accion text not null,
  modulo text not null,
  registro_id bigint,
  descripcion text,
  ip text,

  created_at timestamptz default now()
);

>Activar RLS:
alter table public.bitacora_auditoria enable row level security;

- Como el backend usa supabaseAdmin, no necesitamos políticas públicas.

> 26.2 Crear auditService.js
- Ruta:
backend/src/services/auditService.js

> 26.3 Crear auditController.js
- Ruta:
backend/src/controllers/auditController.js

> 26.4 Crear auditRoutes.js
- Ruta:
backend/src/routes/auditRoutes.js

> 26.5 Actualizar server.js


> 26.11 Pruebas en Postman

> A. Login para generar auditoría
- POST http://localhost:3001/api/auth/login

- Body:
{
  "username": "superadmin",
  "password": "superadmin123*"
}

- Esto debe crear un registro en bitácora con:
accion: iniciar_sesion
modulo: autenticacion

> B. Crear una solicitud pública
- POST http://localhost:3001/api/solicitudes/public

- Body:
{
  "pais_id": 1,
  "nombre": "Persona Prueba",
  "correo": "persona@demo.com",
  "telefono": "3000000000",
  "finalidad": "Servicio",
  "mensaje": "Mensaje de prueba"
}

- Debe crear un registro:
accion: crear_solicitud_publica
modulo: solicitudes_contacto

> C. Listar auditoría
- GET http://localhost:3001/api/auditoria

- Authorization:
Bearer TOKEN_SUPERADMIN

- Respuesta esperada:
[
  {
    "id": 1,
    "accion": "iniciar_sesion",
    "modulo": "autenticacion",
    "registro_id": 1,
    "descripcion": "El usuario superadmin inició sesión",
    "ip": "::1",
    "created_at": "..."
  }
]

> D. Ver detalle de auditoría
- GET http://localhost:3001/api/auditoria/1

- Authorization:
Bearer TOKEN_SUPERADMIN

> E. Editor no puede ver auditoría

- Login como editor_argentina y prueba:
GET http://localhost:3001/api/auditoria

- Respuesta esperada:
{
  "error": "No tienes permisos para realizar esta acción"
}

> Rutas del módulo Bitácora
GET /api/auditoria
GET /api/auditoria/:id


> Paso 27: Archivos / media + Supabase Storage__________________

Este módulo permite dos formas de manejar archivos:

- Registrar una URL manual
  Ejemplo: una imagen alojada en otro sitio.

- Subir un archivo real a Supabase Storage
El backend sube el archivo, obtiene la URL pública, guarda el registro en la tabla archivos y puede asociarlo a noticias o testimonios.


> 27.1 SQL en Supabase

create table if not exists archivos (
  id bigint generated by default as identity primary key,

  nombre_archivo text not null,
  url text not null,
  tipo_archivo text not null,
  modulo text not null,
  referencia_id bigint,
  subido_por bigint references usuarios(id),

  created_at timestamptz default now(),
  updated_at timestamptz default now(),

  constraint archivos_modulo_check
  check (modulo in ('noticias', 'testimonios', 'solicitudes_contacto', 'general'))
);

alter table public.archivos enable row level security;

> 27.2 Crear backend/src/services/archivoService.js
> 27.3 Crear backend/src/controllers/archivoController.js
> 27.4 Crear backend/src/routes/archivoRoutes.js


> Paso 28: Rutas públicas del portal visitante
> 28.1 Crear backend/src/services/publicService.js
> 28.2 Crear backend/src/controllers/publicController.js
> 28.3 Crear backend/src/routes/publicRoutes.js


> Paso 29: Pruebas en Postman
> A. Login como superadmin
POST http://localhost:3001/api/auth/login
{
  "username": "superadmin",
  "password": "superadmin123*"
}

> B. Crear archivo/media
POST http://localhost:3001/api/archivos

- Authorization:
Bearer TOKEN_SUPERADMIN

- Body:
{
  "nombre_archivo": "imagen-noticia-argentina.jpg",
  "url": "https://ejemplo.com/imagen-noticia-argentina.jpg",
  "tipo_archivo": "image/jpeg",
  "modulo": "noticias",
  "referencia_id": 1
}

- Respuesta esperada:
{
  "message": "Archivo registrado correctamente"
}

> C. Listar archivos
GET http://localhost:3001/api/archivos

Authorization:

Bearer TOKEN_SUPERADMIN
> D. Ver detalle de archivo
GET http://localhost:3001/api/archivos/1

- Authorization:
Bearer TOKEN_SUPERADMIN

> E. Editar archivo completo
- PUT http://localhost:3001/api/archivos/1

- Authorization:
Bearer TOKEN_SUPERADMIN

- Body:

{
  "nombre_archivo": "imagen-noticia-argentina-actualizada.jpg",
  "url": "https://ejemplo.com/imagen-actualizada.jpg",
  "tipo_archivo": "image/jpeg",
  "modulo": "noticias",
  "referencia_id": 1
}

> F. Editar archivo parcial
- PATCH http://localhost:3001/api/archivos/1

- Authorization:
Bearer TOKEN_SUPERADMIN

- Body:
{
  "url": "https://ejemplo.com/nueva-url.jpg"
}

> G. Eliminar archivo
- DELETE http://localhost:3001/api/archivos/1

- Authorization:
Bearer TOKEN_SUPERADMIN

- Respuesta esperada:
{
  "message": "Archivo eliminado correctamente"
}

> Pruebas públicas _________________________
 Estas rutas no llevan token.
> H. Listar noticias publicadas por país
- GET http://localhost:3001/api/public/paises/argentina/noticias

- Solo devuelve noticias con:
estado = publicado
pais.slug = argentina

> I. Ver detalle público de noticia

- GET http://localhost:3001/api/public/paises/argentina/noticias/titulo-de-la-noticia
El último segmento es el slug de la noticia.

> J. Listar testimonios publicados por país
- GET http://localhost:3001/api/public/paises/argentina/testimonios

- Solo devuelve testimonios con:
estado = publicado
pais.slug = argentina

> K. Ver detalle público de testimonio

- GET http://localhost:3001/api/public/paises/argentina/testimonios/1

> BLOQUE DE RUTAS

> Archivos / media____________________________
GET     /api/archivos
GET     /api/archivos/:id
POST    /api/archivos
PUT     /api/archivos/:id
PATCH   /api/archivos/:id
DELETE  /api/archivos/:id

> Públicas para portal visitante _____________
GET     /api/public/paises/:paisSlug/noticias
GET     /api/public/paises/:paisSlug/noticias/:noticiaSlug
GET     /api/public/paises/:paisSlug/testimonios
GET     /api/public/paises/:paisSlug/testimonios/:id


> Paso 30: Supabase Storage para imágenes y archivos _______+ _______

> 1. Crear bucket en Supabase

- En Supabase vamos a la ruta:
Storage → New bucket

- Configuramos:
Bucket name: cms-media
Public bucket: ON

> 2. SQL Editor: ajustar tabla archivos

> Ajustamos la tabla archivos:
alter table public.archivos
add column if not exists bucket text default 'cms-media',
add column if not exists storage_path text;

> o si vamos a crear la tabla de nuevo desde cero:
TRUNCATE TABLE archivos RESTART IDENTITY CASCADE;

create table if not exists archivos (
  id bigint generated by default as identity primary key,

  nombre_archivo text not null,
  url text not null,
  tipo_archivo text not null,
  modulo text not null,
  referencia_id bigint,

  bucket text default 'cms-media',
  storage_path text,

  subido_por bigint references usuarios(id),

  created_at timestamptz default now(),
  updated_at timestamptz default now(),

  constraint archivos_modulo_check
  check (modulo in ('noticias', 'testimonios', 'solicitudes_contacto', 'general'))
);

alter table public.archivos enable row level security;

> 3. Instalar multer en una nueva terminal dentro del backend:
npm install multer

> 4. Gestionar los archivos:

middlewares/uploadMiddleware.js
services/archivoService.js
controllers/archivoController.js
routes/archivoRoutes.js
services/noticiaService.js
controllers/noticiaController.js
routes/noticiaRoutes.js
services/testimonioService.js
controllers/testimonioController.js
routes/testimonioRoutes.js

> Pruebas Postman _______________________

> Subir imagen general:
- POST http://localhost:3001/api/archivos/upload

- Body form-data:
file: imagen.jpg
modulo: noticias
referencia_id: 1

> Subir imagen directamente a noticia
- PATCH http://localhost:3001/api/noticias/1/imagen

- Body form-data:
file: imagen-noticia.jpg
z
> Subir foto directamente a testimonio
- PATCH http://localhost:3001/api/testimonios/1/foto

- Body form-data:
file: foto-testimonio.jpg

> subir una imagen JPG
- POST /api/archivos/upload

- Body → form-data:
file            portada.jpg
modulo          noticias
referencia_id   1

- Resultado:
cms-media/noticias/1710000000-portada.jpg

> subir PDF
- POST /api/archivos/upload

- Body → form-data:
file            brochure.pdf
modulo          general
referencia_id   1

- Resultado:
cms-media/general/1710000000-brochure.pdf


### _ CREACIÓN DEL FE_ 

> Abrir una nueva terminal

> Paso 1 FE: crear proyecto frontend
Desde la carpeta raíz del proyecto, al mismo nivel de backend:

- npm create vite@latest frontend

- Seleccionar:
React
JavaScript

- Luego:

cd frontend
npm install
npm install axios react-router-dom bootstrap bootstrap-icons

> Paso 2 FE: estructura inicial

- Dentro de:
frontend/src/

- Dejamos esta estructura inicial...:

src/
├── api/
│   └── axiosClient.js
├── assets/
├── components/
│   ├── public/
│   └── admin/
├── context/
│   ├── AuthContext.jsx
│   └── authContext.js
├── hooks/
│   └── useAuth.js
├── layouts/
│   ├── PublicLayout.jsx
│   └── AdminLayout.jsx
├── pages/
│   ├── public/
│   │   ├── HomePage.jsx
│   │   ├── PublicNoticiasPage.jsx
│   │   ├── PublicTestimoniosPage.jsx
│   │   └── ContactoPage.jsx
│   └── admin/
│       ├── LoginPage.jsx
│       └── DashboardPage.jsx
├── routes/
│   └── ProtectedRoute.jsx
├── App.jsx
├── main.jsx
└── index.css


> Paso 3 FE: configurar .env

- Crear:

frontend/.env

VITE_API_URL=http://localhost:3001/api
VITE_PUBLIC_COUNTRY=argentina

> Paso 4 FE: src/api/axiosClient.js
> Paso 5 FE: src/context/AuthContext.jsx
> Paso 6 FE: src/routes/ProtectedRoute.jsx
> Paso 7 FE: src/main.jsx
> Paso 8 FE: src/App.jsx
> Paso 9 FE: src/layouts/PublicLayout.jsx
> Paso 10 FE: src/layouts/AdminLayout.jsx
> Paso 11 FE: src/pages/public/HomePage.jsx
> Paso 12 FE: src/pages/admin/LoginPage.jsx
> Paso 13 FE: src/pages/admin/DashboardPage.jsx
>             src/pages/admin/ProfilePage.jsx
>             src/pages/admin/ChangePasswordPage.jsx
>             src/pages/admin/SecurityQuestionPage.jsx
>             src/pages/admin/ForgotPasswordPage.jsx
>             ...

> Paso 14 FE: páginas públicas base_______________________
> src/pages/public/PublicNoticiasPage.jsx 
> src/pages/public/PublicTestimoniosPage.jsx
> src/pages/public/ContactoPage.jsx

> Paso 15 FE: src/index.css

> Paso 16 FE: ejecutar frontend
npm run dev


> El módulo de autenticación del frontend.

- Debe incluir estas pantallas/funciones:

/admin/login
/admin/profile
/admin/change-password
/admin/security-question
/admin/forgot-password

- Y consumirá:

POST  /api/auth/login
PATCH /api/auth/me
PATCH /api/auth/change-my-password
PATCH /api/auth/security-question
POST  /api/auth/security-question
POST  /api/auth/forgot-password

- El orden sería:

1. Login
2. Recuperar contraseña
3. Actualizar mi perfil
4. Cambiar mi contraseña
5. Configurar pregunta de seguridad
6. Luego dashboard

> Módulo login
> Módulo Noticias
> Módulo Testimonios
> Módulo solicitudes de contacto
> Módulo Dashboard
> Módulos públicos por país
